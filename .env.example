# API base URL for the TrustEats backend.
# Leave unset to use the default deployment (https://trusteatslatest.onrender.com).
VITE_API_BASE_URL=https://trusteatslatest.onrender.com/api/v1

# Optional: a bearer token to attach in development when the API uses token auth.
# VITE_DEV_AUTH_TOKEN=
